kind = "web"
previewPath = "/"
title = "PickFlix"
version = "1.0.0"
id = "artifacts/pickflix"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 20057

[services.development]
run = "pnpm --filter @workspace/pickflix run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/pickflix", "run", "build" ]
publicDir = "artifacts/pickflix/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "20057"
BASE_PATH = "/"
